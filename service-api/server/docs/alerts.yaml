paths:
  /alerts:                # path of the core permission from your endpoint
    get:                 # endpoint request type (post request)
      tags:               # Tag property
        - Alerts            # Value of the tag
      summary: gets all Alerts
      security:
        - bearerAuth: []   # Use OAuth with a different scope
        - code: []      
      produces:
      - application/json
      responses:          # server responses
        200:
          description: An array of Alerts   